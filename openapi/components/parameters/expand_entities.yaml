name: expand
in: query
required: false
description: |
  Expand related entity resources inline. Supports the entity expandable fields
  documented by the API validator for entity list and retrieve operations.
style: form
explode: true
schema:
  type: array
  items:
    type: string
    enum:
      - connect
      - credit_score
      - attribute
      - vehicle
      - phone_latest_verification_session
      - identity_latest_verification_session

